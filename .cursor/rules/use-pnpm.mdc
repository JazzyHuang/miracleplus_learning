---
description: 强制使用 pnpm 作为包管理器，禁止使用 npm
alwaysApply: true
---

# 只能使用 pnpm

本项目使用 pnpm 作为唯一的包管理器。

- 安装依赖：`pnpm install` 或 `pnpm add <package>`
- 运行脚本：`pnpm dev`、`pnpm build`、`pnpm lint`
- 禁止使用 `npm install`、`npm run`、`npx` 等 npm 命令
- 如需执行包命令，使用 `pnpm dlx` 替代 `npx`

```bash
# ❌ BAD
npm install
npm run dev
npx create-next-app

# ✅ GOOD
pnpm install
pnpm dev
pnpm dlx create-next-app
```
